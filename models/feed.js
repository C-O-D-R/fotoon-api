// Libraries
const mongoose = require('mongoose');

//Feed Schema
const FeedSchema = new mongoose.Schema({



})

// Export
const model = mongoose.model('FeedSchema', FeedSchema);
module.exports = model;